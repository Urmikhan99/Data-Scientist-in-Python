import numpy as np

python_list = [1,2,3,4,5]

import numpy as np 
numpy_array=np.array([1,2,3,4,5])
print(numpy_array)
'''
NumPy Array → More powerful than lists:
Supports mathematical operations directly (e.g., numpy_array * 2).
Stores elements of the same data type (efficient).
Provides fast computation (vectorized operations).

👉 Difference: Python lists are flexible but slower,
 while NumPy arrays are optimized for mathematical and 
 scientific computing.
'''